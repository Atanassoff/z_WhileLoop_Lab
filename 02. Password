function demo(input){
    let name = input[0];
    let pass = input[1];
    let i = 2;
    let command = "";
    while((command = input[i]) != pass){
        i++;
    }
    console.log(`Welcome ${name}!`);
}
