function demo(input){
    let num = 0;
    let max = -1000000000;
    let command = "";
    let i = 0;
    while((command = input[i]) != "Stop"){
        num = Number(command);
        if(max < num){max = num;}
        i++;
    }
    console.log(max);
}
